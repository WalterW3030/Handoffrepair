# Seed table — DERIVED FROM configs/sizing.yaml (single source of truth).
# Column seeds are read from sizing.yaml:seeds_calibrated / seeds_heldout at runtime.
master_seed: 20260811
user_simulator: 101
episode_sampler: 202
extraction_audit_sampler: 303
# per-column seed lists: see sizing.yaml (calibrated pairs: [1,2]; held-out pair: [1])
